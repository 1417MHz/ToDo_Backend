export interface ITodoList {
    post_id: number,
    user_id: number,
    contents: string,
    status: number,
    create_date: string
}

// export interface IUpdateTodoList extends ITodoList {
//     // 게시물 만의 고유 코드가 필요?
// }
